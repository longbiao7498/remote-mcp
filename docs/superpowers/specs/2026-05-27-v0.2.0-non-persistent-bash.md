# remote-mcp v0.2.0 设计规范：非持久 Bash

**日期**：2026-05-27  
**状态**：待审核 → 待实施  
**前身**：增量于 [`2026-05-26-remote-mcp-design.md`](./2026-05-26-remote-mcp-design.md)（v2, 已实施为 v0.1.x）；本规范覆盖 v0.2.0 的架构转向，其他设计（连接生命周期、SFTP 工具、9 个非 Bash 工具）继续以 v2 spec 为准。

---

## 1. 概述与目标

v0.2.0 把 Bash 工具从**持久 bash session 模型**改为**每次调用独立 channel 的非持久模型**，行为对齐 Claude Code 原生 Bash 工具。

**核心目标**（沿用项目原始目标 §1）：让 Claude Code 通过 MCP 操控远程 Linux 服务器，**使用体验对齐到不会因 schema/输出格式/行为差异导致 agent 用错工具**。

v0.1.x 在这条目标上有一个长期未发现的盲点：**Bash 工具的"shell 持久性"与 Claude Code 原生工具行为不一致**——原生每次调用是新 shell，我们的是持久 session。这个盲点带来：

1. 一类系统性 bug（srun 等 stdin-watcher 程序在持久 PTY 上必然挂死）
2. agent 心智负担（"我上一调用 export 的变量这次还在吗？cd 了吗？"）
3. ~350 LOC 的支撑机制（sentinel 协议、PTY 分配、reader thread、setsid 包装等）

v0.2.0 通过架构转向同时解决这三件事。

## 2. 与 v1 spec 的关系

| v1 spec 段 | v0.2.0 对其影响 |
|------------|---------------|
| §1 目标 | **不变**——v0.2.0 进一步落实"对齐原生体验"原则 |
| §2 硬性约束 | **不变** |
| §3.6 持久 bash session 决策 | **被本规范替代**——选了非持久路径 |
| §5.1 connection.py | 微调：删除 `get_bash_session()`，新增 snapshot 管理 |
| §5.1.1 连接生命周期 | 微调：拓扑图里 bash channel 从"常驻"变为"per-call ephemeral" |
| §5.2 bash_session.py + sentinel 协议 | **整段废弃**——文件删除，sentinel 协议不再需要 |
| §5.3.7 Bash 工具 | **重写**——核心实现从"调 BashSession.execute()"改为"per-call exec_command + snapshot 包装" |
| §5.3.10 Feedback、§5.3.1-§5.3.6/§5.3.8/§5.3.9 其他工具 | **不变** |
| §5.4 server.py | 极小修订——dispatch 路径不变；可能微调 `_with_retry` 的异常分类 |
| §6 接口规范 | Bash 行的 schema 不变（参数名相同），输出格式有微调 |
| §7 带宽与延迟优化 | 增加一行：Bash 调用新增 channel-open + bash-startup 开销，是 v0.2.0 的**已接受代价** |
| §9 错误处理与重连 | reconnect WARNING 简化（不再有"shell 状态丢失"语义） |
| §14 已知局限 | 部分条目删除（"PID 复用"等 background bash 边界仍在；"持久 bash 引发的边界"集体消失） |
| §15 未来工作 | 已追加两条（流式输出、可选 `mode: persistent`）作为 v0.2.0 的反向出口 |

**v2 spec 保留为权威设计**；本规范是其 v0.2.0 增量。读者应先读 v2 再读本文。

## 3. 动机（目标导向重述）

之前的设计推理走过这样一条路：

```
v1（原始决策）：
  "持久 bash 让 cwd/env 跨调用持久，agent UX 更好" → 选了持久

v0.1.0-v0.1.2（逐渐暴露的代价）：
  - sentinel 协议（必要，因为持久 stdout 无命令边界）
  - PTY 分配（必要，因为没 PTY 时 \x03 不发 SIGINT）
  - reader thread（必要，避免缓冲区死锁）
  - setsid 包装后台命令（必要，否则 kill PGID 会误杀 BashSession）
  - </dev/null 包装（必要，否则 srun 类 stdin-watcher 挂死）
  - timeout 时返回 partial output（必要，否则信息丢失）
  
v0.2.0 触发点（agent 反馈 + 实测）：
  - 反馈：srun + timeout-丢-stdout 的复合诊断
  - 实测：Claude Code 原生 Bash 每次重置 cwd/env，跟我们持久行为不同
  - 反思：v1 §3.6 把"持久 bash"当作设计基石，但其实它服务的是
         "人类视角的便利"而非"agent 视角的对齐"；目标里说的是
         后者，所以原决策与目标其实不一致
```

→ v0.2.0 的转向不是修一个 bug，是**修一个 v1 时的设计前提错误**。

## 4. 架构转向

### 4.1 SSH 三层中谁动谁不动

```
┌──────────────────────────────────────────────────────────┐
│  TCP 连接 — 不变（一辈子 1 个）                            │
│  SSH Transport — 不变（一辈子 1 个；compress, keepalive）  │
│  ├─ Channel: SFTP — 不变（懒初始化，复用）                  │
│  ├─ Channel: persistent bash — 删除                       │
│  └─ Channel: exec ×N — 不变 + 范围扩大                     │
│      │   (Glob/Grep/MultiRead/Read sed-slice)             │
│      └─ 新成员：Bash 每次调用 ← 这是 v0.2.0 的全部架构改动  │
└──────────────────────────────────────────────────────────┘
```

**Bash 从"占一个常驻 channel"变成"每次开一个一次性 channel，跟 Glob/Grep 同模型"。**

### 4.2 删除清单

| 项 | 当前用途 | 删除理由 |
|------|---------|---------|
| `remote_mcp/bash_session.py` 整个文件（~200 LOC） | sentinel 协议、reader thread、cwd 缓存 | 模型不需要了 |
| `connection.py` 中的 `get_bash_session()` 方法和 `_bash_session` 字段 | 提供 BashSession 单例 | 同上 |
| sentinel 解析（`parse_sentinel_line`、`_SENTINEL_RE`） | 持久 stdout 流的命令边界检测 | per-call channel 天然有边界（channel 关闭 = 命令结束） |
| PTY 分配（`get_pty(term="dumb")` + `stty -echo` + 整套 init 序列） | 让 `\x03` 真正发 SIGINT | timeout 改用 `channel.close()`，不需要 PTY |
| `set +m / set +o histexpand / export PS1=''` 初始化序列 | 让 sentinel 输出干净 | 没 sentinel 了 |
| `tests/integration/test_bash_session.py` | BashSession 行为测试 | 模块不存在了 |
| `tests/unit/test_sentinel_parser.py` | sentinel 解析单元测试 | 同上 |
| `tools/bash.py` 中的 `_match_line_numbers` 调用（在 timeout 路径外） | partial output 收集逻辑 | 部分逻辑迁移到新实现；旧实现整体重写 |

**估算删除：~280 LOC + 2 个测试文件**。

### 4.3 保留清单

| 项 | v0.2.0 中如何使用 |
|------|----------------|
| `SSHConnection` 类、`exec()`、`get_sftp()`、`exec_with_retry()` | 不变 |
| Transport 持久化、`compress=True`、`set_keepalive()` | 不变 |
| `_do_reconnect()` 和 `check_and_clear_reconnect_flag()` 机制 | 微调（不再需要 invalidate BashSession） |
| ProxyJump | 不变 |
| 9 个非 Bash 工具（Read/Write/Edit/MultiEdit/MultiRead/FileStat/Glob/Grep/Feedback + 新加的 Upload/Download/RemoteInfo） | 不变 |
| Bash 工具的 `run_in_background=true` 路径 + setsid 进程组语义 + PID/log 返回格式 | 不变（实现细节微调，使用 per-call exec 启动） |
| Bash 工具 schema（参数名、参数类型、默认值） | 不变 |
| server.py `_with_retry` + 重连前置 WARNING 机制 | 不变（WARNING 文本简化） |

### 4.4 新增清单

| 项 | 作用 |
|------|------|
| `connection.py` 中的 snapshot 管理：`_snapshot_path` 字段、`_create_snapshot()` 方法、`close()` 中的 snapshot cleanup | 实现"复刻 CC 原生的 snapshot 机制"（§5.1） |
| `tools/bash.py` 重写：`_bash_foreground()` 用 per-call exec_command + snapshot 包装；`_bash_background()` 类似改写；`_drain_channel_partial()` 用于 timeout 路径收集 partial output | 新模型的核心实现 |
| `tests/integration/test_bash_tool.py` 大幅重写 | 测试新行为 |
| `docs/explanation/why-non-persistent-bash.md`（en+zh） | 永久记录"为什么走非持久"的设计推理 |

## 5. 关键决策（含考虑过的备选方案）

### 5.1 Snapshot 机制：复刻 Claude Code 原生

**决策**：SSHConnection 建立时执行一次 `bash -ic 'declare -p; declare -fp; alias 2>/dev/null'`，把结果写到远程 `/tmp/rmcp-snapshot-<host>-<pid>.sh`；每次 Bash 调用 `source` 此文件。

**为什么**：通过实测确认 Claude Code 原生 Bash 用的就是这套方法（见命令 `ps -p $$ -o args` 输出里的 `source /home/lb/.claude/shell-snapshots/snapshot-bash-*.sh`）。目标章节"对齐原生体验"要求我们复刻它。

**考虑过的备选方案**：

| 备选 | 优点 | 缺点 | 为什么不选 |
|------|------|------|----------|
| **A**：`bash --norc --noprofile -c <cmd>` | 最简单 | 丢失用户 PATH、aliases、conda init、`module load` 等环境 | 对集群/科研用户体验大幅下降；与 CC 原生不一致 |
| **B**：每次调用都 `bash -ic 'source bashrc; <cmd>'`（每次源 bashrc） | 简单，环境完整 | 每次都付 bashrc 启动开销（可能 100-500ms） | 性能不可接受 |
| **C（采用）**：snapshot + replay | 一次性付 bashrc 启动；后续每次便宜地 source snapshot；与 CC 原生一致 | 实现复杂；需 snapshot 文件生命周期管理 | 收益最大、与 CC 对齐 |

**Snapshot 创建命令**（在 SSHConnection.connect() 末尾执行）：

```bash
bash -ic 'declare -p 2>/dev/null; declare -fp 2>/dev/null; alias 2>/dev/null' \
  > /tmp/rmcp-snapshot-<host_name>-<server_pid>.sh
```

- `-i` interactive 让 `~/.bashrc` 被 source
- `-c` 一次性运行
- `declare -p` 导出全部已 declare 的变量（含 export/readonly 标记）
- `declare -fp` 导出全部函数定义
- `alias` 导出全部别名
- `2>/dev/null` 抑制无关警告
- snapshot 路径含 `<host_name>` 和 `<server_pid>`，避免多个 MCP 实例冲突

**Snapshot 使用命令**（每次 Bash 调用包装）：

```bash
bash --noprofile --norc -c "source /tmp/rmcp-snapshot-<host>-<pid>.sh 2>/dev/null || true; <user_cmd>" </dev/null
```

- `--noprofile --norc` 不再 source rc 文件（snapshot 已经包含其结果）
- `source ... || true` snapshot 不存在或源出错时 silently 继续（容错）
- `</dev/null` stdin 重定向到 /dev/null（解决 srun 类问题，与 CC 原生一致）

**Snapshot 生命周期**：

- 创建：`SSHConnection.connect()` 末尾，建立 SSH 后立即创建
- 重建：`SSHConnection._do_reconnect()` 后重新创建（远程 `/tmp` 可能仍有旧 snapshot 但稳妥起见重建）
- 清理：`SSHConnection.close()` 中 `rm -f` 删除；进程异常退出时依赖 `/tmp` 重启清理

**失败容错**：

- 若 snapshot 创建失败（用户 bashrc 报错、`/tmp` 不可写、网络抖动等），**记 warning 但不致命**——后续 Bash 调用退化为不带 snapshot 的纯 `bash --noprofile --norc -c`。
- agent 仍能用，只是 PATH 等不含用户自定义。文档明说这个降级行为。

### 5.2 Cwd 不持久

**决策**：每次 Bash 调用都从远程 sshd 默认 cwd（通常 `$HOME`）开始。不尝试记忆"agent 上次 cd 去了哪里"。

**为什么**：

1. **与 CC 原生一致**——实测 `cd /tmp` 后下一调 `pwd` 回到 home（实际 harness 还会重置到项目根，但精确策略我们不复制，因为我们没有"项目"概念）
2. **降低 agent 心智负担**——agent 不需要在心里维护"我在哪台主机的哪个目录"
3. **强迫 agent 使用 `cd dir && cmd` 的复合形式**——这是 CC 原生用户的标准模式

**Bash 工具输出格式微调**：

旧：`[host=X cwd=Y]\n<output>`  
新：`[host=X]\n<output>`（删除 `cwd=` 部分，因为它每次都是固定值——agent 知道是 home，没意义）

**保留 host=X**：多主机场景下 agent 需要看清当前调用的是哪台主机，这条仍然有价值。

### 5.3 Timeout 用 `channel.close()` 而非 PTY Ctrl-C

**决策**：Bash 前台模式超时时调用 `channel.close()`；远程 sshd 检测到 channel 关闭后会给远程命令的 session 发 SIGHUP。

**为什么**：

1. **不需要 PTY**——v0.1.x 引入 PTY 的唯一理由就是让 `\x03` 生效。non-persistent 模型下 channel.close() 直接干掉远程进程，不需要 PTY 黑魔法
2. **更干净**——SIGHUP 比 SIGINT 更"决绝"；不会出现"命令吞掉 SIGINT 继续运行"的情况（如果用户命令显式 trap'd SIGINT 的话）
3. **更彻底**——SIGHUP 会传播到 session 内所有子进程（不像 SIGINT 只到前台进程组），所以命令派生的子进程也会被清理

**Background bash 不受影响**：因为它用 `setsid nohup`，进程脱离了 channel 的 session，channel 关闭也不影响后台进程——这正是 background 模式想要的行为。

### 5.4 Partial output 流式收集

**决策**：超时时返回**已经从 channel 收到的所有 stdout**，附在 `Error: Command timed out` 之前。

**实现方式**：

```python
# 简化伪代码（实际实现见 plan）
stdin, stdout, stderr = client.exec_command(wrapped_cmd, timeout=...)
out_buf = b""
deadline = time.time() + timeout
while not stdout.channel.exit_status_ready():
    if time.time() > deadline:
        # Timeout: drain whatever's available, then close
        while stdout.channel.recv_ready():
            out_buf += stdout.channel.recv(4096)
        stdout.channel.close()
        return f"[host=X]\n{out_buf.decode()}\n\nError: Command timed out after Ns on X"
    if stdout.channel.recv_ready():
        out_buf += stdout.channel.recv(4096)
    else:
        time.sleep(0.05)
# Normal exit: read remaining, return with exit code
out_buf += stdout.read()
return ...
```

**为什么**：

1. 上一轮 agent feedback 里 Bug 2 的核心痛点就是 timeout 丢 stdout
2. Per-call exec 模型下这是**自然实现**——不像持久模型要靠 sentinel 协议绕一圈

### 5.5 Snapshot 与 Reconnect 的交互

**决策**：`SSHConnection._do_reconnect()` 流程末尾**重建 snapshot**——即新建一个 `/tmp/rmcp-snapshot-<host>-<pid>.sh`（PID 不变所以路径相同，会覆盖旧文件）。

**为什么**：

1. SSH 断了重连后，远程 `/tmp` 文件可能还在（只是连接换了）。理论上不重建也能用。
2. 但用户的 bashrc 可能在断线期间被修改；重建 snapshot 让 agent 总是看到最新环境，是更稳妥的语义。
3. 重建是一次性开销（~100-500ms），重连本来就是异常路径，多这点开销可接受。

### 5.6 Reconnect WARNING 简化

**决策**：reconnect 后的 WARNING 文本从 v0.1.x 的"shell state was reset; cwd is now $HOME, env vars lost, use absolute paths" 简化为"SSH connection to `<host>` was lost and has been re-established. Snapshot was rebuilt; if your bashrc has changed since the connection started, the new state takes effect from this point."

**为什么**：v0.1.x 那段警告假定了"agent 有累积的 shell 状态需要重新建立"。v0.2.0 没有这种累积状态了——每次调用本来就是 fresh shell。所以 WARNING 不再需要"教 agent 重做 setup"，只需要"通知 agent 链路有过中断"。

## 6. Agent-visible Breaking Changes

按 agent 实际能观察到的变化列出，agent 训练数据如果依赖这些行为会失效：

| 变化 | v0.1.x 行为 | v0.2.0 行为 | Agent 应当如何适应 |
|------|-----------|-----------|------------------|
| cwd 跨调用 | `cd /tmp` 后下一调 `pwd` 是 `/tmp` | `cd /tmp` 后下一调 `pwd` 是 `$HOME` | 用 `cd /tmp && cmd` 单次拼接 |
| env 跨调用 | `export FOO=bar` 后下一调 `echo $FOO` 是 `bar` | 是空 | 用 `FOO=bar cmd` 内联，或 `bash -c "export FOO=bar; cmd"` 单次包装 |
| source venv | `source venv/bin/activate` 后下一调 `python` 用 venv | `python` 还是系统 python | `venv/bin/python script.py` 绝对路径，或 `. venv/bin/activate && python ...` 单次拼接 |
| ssh-agent | `eval $(ssh-agent); ssh-add` 后下一调 `git pull` 用 agent | 不再有 agent；下一调失败 | 用无 passphrase 的 deploy key，或单次内 setup+use 拼接 |
| Bash 输出前缀 | `[host=X cwd=Y]\n<output>` | `[host=X]\n<output>` | 一般不依赖 cwd 部分；删除无影响 |
| Reconnect WARNING 文本 | 详细的"state was reset"指引 | 简化为"connection was reset" | agent 不需要做特殊事情；旧的"重做 setup"建议不再适用但也无害 |
| 单次 Bash 调用延迟 | < 50ms typical | 多 100-500ms（VPN）或 500-1000ms（慢 FS） | agent 倾向于把多个相关命令用 `&&` 拼成单次调用，减少调用次数 |

## 7. 接受的取舍

### 7.1 性能代价

**每次 Bash 调用比 v0.1.x 慢 100-1000ms**（具体值看 RTT 和远程 FS 速度）。

- 局域网（RTT 1ms）+ 本地 FS：+50-150ms（主要是 bash 进程启动）
- 普通 VPN（RTT 50-200ms）：+200-600ms
- 跨洲 VPN（RTT 500ms+）+ 慢共享 FS：+1-2s

**为什么接受**：

1. 单次延迟可量化、可文档化
2. CC 原生在同样条件下付同样代价；agent 设计本来就考虑了这点
3. agent 适应方式（多命令 `&&` 拼接）反而带来正面效果（减少总往返次数）
4. v0.1.x 的"无延迟"是 sentinel + PTY + reader thread + setsid + 一堆补丁堆出来的；削减复杂度本身是巨大收益
5. **future work** 已记录 opt-in `mode: persistent` 作为反向出口——如果将来有用户对慢 FS 实在不能容忍，可重新启用持久模式

### 7.2 部分工作流被破坏

ssh-agent / conda activate / nvm use / source venv 这类"设状态供后续使用"的命令，跨调用不再生效。

**为什么接受**：

1. 这些 use case 在远程 agent 场景里实际频率低
2. 全部都有可接受的 workaround（内联 setup+use、绝对路径）
3. CC 原生用户已经适应这种模式
4. CLAUDE.md.fragment.md 会显式给出替代写法，agent 学习成本不高

### 7.3 第一次连接 + snapshot 创建多 ~500ms

`SSHConnection.connect()` 末尾的 snapshot 创建是 `bash -ic` 启动 + dump，远程 bashrc 复杂的话可能 100-500ms。

**为什么接受**：

1. 一次性开销，被整个 MCP server 生命周期摊薄
2. Reconnect 时重建多付一次，但 reconnect 本身就是异常路径

## 8. 明确不做（指向 future work）

| 项 | 为什么不做 | 在哪里记录 |
|---|----------|---------|
| 流式输出（agent 边跑边看 stdout） | MCP 协议层还没支持 | v1 spec §15.2（已于本次讨论中追加） |
| Opt-in `mode: persistent` 配置 | YAGNI——等真用户痛了再做 | v1 spec §15.2（已追加） |
| 跨调用 cwd 持久（即使非持久 bash 也可以"假装" cwd 持久） | CC 原生不做；做了会与目标"对齐原生"冲突 | 本规范 §5.2 |
| `interactive: true` 参数让 stdin 不重定向 /dev/null | YAGNI；CC 原生没这个；真有需要用 `Bash + tmux send-keys` 模式 | 本规范不实现 |
| 自动重试 snapshot 创建失败 | 降级到无 snapshot 已经够用 | 本规范 §5.1 容错小节 |

## 9. 文档级联更新清单

实施 v0.2.0 时必须同步更新的文档（避免出现新行为但旧文档的矛盾）：

| 文档 | 改动 |
|------|------|
| `CHANGELOG.md` / `.zh.md` | 新增 `[0.2.0]` 段，**显眼标注 BREAKING CHANGES**，列 §6 表格内容 |
| `CLAUDE.md` / `.zh.md` | "Persistent bash session + Sentinel protocol" 段整段删除/改写；新增对 snapshot 机制的简述 |
| `CLAUDE.md.fragment.md` / `.zh.md` | 新增/改写 "Shell 操作" 段：删除"shell state persists across calls"假设；加入 `cd && cmd`、`FOO=bar cmd`、`venv/bin/python` 三种推荐模式 |
| `docs/explanation/architecture.md` / `.zh.md` | 拓扑图：删除 "persistent bash channel"；说明 Bash 加入 per-call exec channel 模式 |
| `docs/explanation/design-decisions.md` / `.zh.md` | 新增 "Non-persistent vs persistent bash" 决策段，引用本规范 |
| `docs/explanation/reconnect-and-warning.md` / `.zh.md` | 更新 WARNING 文本对应描述；删除"bash session was rebuilt, state lost"语义 |
| `docs/explanation/why-non-persistent-bash.md` / `.zh.md` | **新建**——本规范的精简版，目标读者是未来想"为什么不持久 bash"的人 |
| `docs/reference/tools/bash.md` / `.zh.md` | 重写 "Behavior notes"：no PTY、stdin 是 /dev/null、cwd 每次重置、错误格式 `[host=X]` 而非 `[host=X cwd=Y]` |
| `docs/reference/config-schema.md` / `.zh.md` | 字段 `bash_timeout_default` 描述保持；可考虑新增 `snapshot_path` 字段（默认 `/tmp/rmcp-snapshot-{host}-{pid}.sh`） |
| `docs/reference/errors.md` / `.zh.md` | Bash 错误条目更新；新增"snapshot 创建失败"warning（如果暴露给 agent 的话） |
| `docs/superpowers/specs/2026-05-26-remote-mcp-design.md` (v2) | 在适当位置加 link 指向本规范，说明 §3.6 / §5.2 / §5.3.7 被 v0.2.0 取代 |

## 10. 验收标准

按"agent 视角"和"开发者视角"分两组：

### 10.1 Agent 视角（黑盒）

- [ ] **`mcp__remote-<host>__Bash("echo hello")`** 返回 `[host=<host>]\nhello\n`，无 `cwd=` 部分
- [ ] **`Bash("cd /tmp")` 后** `Bash("pwd")` 返回 `[host=<host>]\n<HOME>` —— cwd 不持久
- [ ] **`Bash("export FOO=bar")` 后** `Bash("echo $FOO")` 返回 `[host=<host>]\n` —— env 不持久
- [ ] **`Bash("srun -p debug6 -A penglin -N1 -n1 hostname")`** 在合理时间内返回带 hostname，不再永久挂起（前提：用户配 slurm 集群）
- [ ] **`Bash("cat")` (无参数读 stdin)** 立刻返回（EOF），不再永久挂起
- [ ] **`Bash("echo START; sleep 999", timeout=2)`** 返回包含 `START` + `Error: Command timed out after 2s` —— partial output 不丢
- [ ] **`Bash("which python")`** 找到用户 PATH 里的 python（snapshot 起作用），而不仅是系统 default
- [ ] **`Bash("alias")` 或调用用户定义的 alias** 能工作（snapshot 保留 alias）
- [ ] **`Bash("...", run_in_background=true)`** 返回 PID + log path + 4 行操作命令——行为与 v0.1.x 一致
- [ ] **断 SSH 触发 reconnect 后**下一调用结果带 `[WARNING] SSH connection to <host> was lost and has been re-established. Snapshot was rebuilt;` 前缀，**不再**有"shell state was reset"或"cwd is now $HOME"的描述

### 10.2 开发者视角（白盒）

- [ ] **`remote_mcp/bash_session.py` 不存在**
- [ ] **`remote_mcp/connection.py::SSHConnection` 没有 `get_bash_session()` 方法或 `_bash_session` 字段**
- [ ] **`remote_mcp/connection.py::SSHConnection` 有 `_snapshot_path` 属性和 `_create_snapshot()` 方法**；`connect()` 末尾调用 `_create_snapshot()`，`close()` 末尾删除 snapshot 文件
- [ ] **`tools/bash.py` 不再 import `bash_session`**；用 `conn.exec_with_retry()` 或类似 per-call 路径
- [ ] **PTY 相关代码（`get_pty`、`stty -echo`）**全部移除
- [ ] **测试套件全绿**——`pytest tests/` 全通过；删除的测试文件移除对应文件；新行为有对应测试
- [ ] **`docs/explanation/why-non-persistent-bash.md`** 存在（en+zh），内容覆盖本规范第 3 节"动机"
- [ ] **CHANGELOG v0.2.0 段含 BREAKING CHANGES 标记**和 §6 表格内容
- [ ] **CLAUDE.md.fragment.md 更新**为非持久语境，含 §6 三种推荐写法

## 11. 实施提示（详情见 plan）

按 dependency 顺序：

1. **HostConfig 加 snapshot_path 配置字段**（可选，建议有默认值不强制配置）
2. **SSHConnection 加 snapshot 管理**（`_create_snapshot()` + `connect()` 中调用 + `close()` 中清理 + `_do_reconnect()` 中重建）
3. **tools/bash.py 重写** —— `_bash_foreground()` + `_bash_background()` 改用 per-call exec_command + snapshot wrap + /dev/null
4. **删除 bash_session.py + 相关测试文件**
5. **测试 `test_bash_tool.py` 重写**——加 §10.1 验收点对应的测试
6. **文档级联更新**（§9 清单）
7. **CHANGELOG v0.2.0 段**

每步独立可测，TDD 推进。

## 附录：术语

- **持久 bash session**：v0.1.x 模型——一个 bash 进程通过 PTY 接收命令注入，跨工具调用复用。**v0.2.0 已废弃**。
- **non-persistent bash**：v0.2.0 模型——每次 Bash 工具调用都是一个独立的 `exec_command` channel，bash 进程一次性。
- **snapshot 机制**：在远程主机上一次性导出 bashrc 加载后的 shell 环境到一个文件，后续每次 Bash 调用 `source` 该文件——既保留用户环境又避免每次启动 bashrc 开销。
- **per-call channel**：每次工具调用都新开一个 SSH channel，命令结束后关闭——与 Glob/Grep 等工具的现有模式相同。
